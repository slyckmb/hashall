# Moved

Schema authority is documented in `docs/architecture/SYSTEM.md`.
Runtime schema source-of-truth is `src/hashall/migrations/*.sql`.

Historical snapshot preserved at:
`docs/archive/2026-doc-reduction/snapshot/docs/architecture/schema.md`
